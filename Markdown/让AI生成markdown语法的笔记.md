我们被要求将上一个回答中的markdown笔记内容用代码块包起来，以便直接复制。也就是说，需要生成一个包含markdown格式内容的代码块，但不要执行或渲染它。注意用户说“将上述回答的markdown语法笔记用代码块包起来”，意思是用三个反引号围住，但为了防止代码块中包含反引号，可能需要使用四个反引号。最外层是助手输出。我们只需生成：


或者用copilot让智能体直接帮我优化markdown语法